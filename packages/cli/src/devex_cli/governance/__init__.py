"""governance package — Work ID validation utilities.

Mirrors the logic in packages/workflow-framework/src/governance/work-id.ts
so that the CLI enforces the same rules locally that the CI pipeline enforces
remotely.
"""
